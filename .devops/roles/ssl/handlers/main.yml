---
# Handlers da role ssl (reutiliza os do nginx)

- name: Reload Nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded

- name: Restart Nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted
